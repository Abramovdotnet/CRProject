<UserControl x:Class="CRProjectEditor.Views.WorldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CRProjectEditor.Views"
             xmlns:converters="clr-namespace:CRProjectEditor.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="{StaticResource PrimaryBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.3*" MinWidth="250"/>
                    <ColumnDefinition Width="0.7*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Scene Details & Actions -->
                <Border Grid.Column="0" Background="{StaticResource TertiaryBackgroundBrush}" Padding="10" Margin="0,0,5,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!-- Scene Type Drag Buttons -->
                            <TextBlock Text="Drag to Create Scene:" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,10"/>
                            <ItemsControl ItemsSource="{Binding AvailableSceneTypesForDrag}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource DraggableSceneTypeBorderStyle}" MouseDown="SceneTemplate_MouseDown">
                                            <TextBlock Text="{Binding}" Style="{StaticResource SmallTextBlockStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <Border Height="1" Background="{StaticResource PrimaryHueMidBrush}" Margin="0,20,0,20"/>

                            <!-- Selected Scene Details & Connection UI -->
                            <StackPanel Visibility="{Binding MapSelectedScene, Converter={StaticResource NullToVisibilityConverter}}">
                                <TextBlock Text="Selected Scene:" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,5"/>
                                <TextBlock Text="{Binding MapSelectedScene.Name, FallbackValue='N/A', StringFormat='Name: {0}'}" Style="{StaticResource SmallTextBlockStyle}" Margin="0,0,0,2}"/>
                                <TextBlock Text="{Binding MapSelectedScene.SceneType, FallbackValue='N/A', StringFormat='Type: {0}'}" Style="{StaticResource SmallTextBlockStyle}" Margin="0,0,0,2}"/>
                                <TextBlock Text="{Binding MapSelectedScene.Id, FallbackValue='N/A', StringFormat='ID: {0}'}" Style="{StaticResource SmallTextBlockStyle}" Margin="0,0,0,10}"/>

                                <TextBlock Text="Connect to:" Style="{StaticResource SmallHeaderTextBlockStyle}" Margin="0,10,0,5}"/>
                                <ComboBox ItemsSource="{Binding AllOtherScenesForConnection}"
                                          SelectedItem="{Binding SelectedSceneForConnection}"
                                          DisplayMemberPath="NameAndId"
                                          IsEnabled="{Binding MapSelectedScene, Converter={StaticResource NullToBooleanConverter}}"
                                          Margin="0,0,0,5}"
                                          Style="{StaticResource CustomComboBoxStyle}"/>
                                <Button Content="Add Connection" Command="{Binding AddConnectionCommand}" Style="{StaticResource CustomButtonStyle}" Margin="0,0,0,5}"/>
                                <Button Content="Delete All Connections" Command="{Binding DeleteAllConnectionsCommand}" Style="{StaticResource CustomButtonStyle}" Margin="0,0,0,5}"/>
                                <Button Content="Delete Scene" Command="{Binding DeleteSelectedSceneCommand}" Style="{StaticResource CustomButtonStyle}" Margin="0,0,0,10}"/>
                            </StackPanel>

                            <Border Height="1" Background="{StaticResource PrimaryHueMidBrush}" Margin="0,20,0,20"/>
                            
                            <Button Content="Save Current Layout" Command="{Binding SaveCurrentMapLayoutCommand}" Style="{StaticResource CustomButtonStyle}" Margin="0,10,0,5}"/>
                            <Button Content="Generate Coordinates" Command="{Binding GenerateCoordinatesCommand}" Style="{StaticResource CustomButtonStyle}" Margin="0,5,0,5}"/>

                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Right Panel: DataGrid and Interactive Map -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DataGrid Grid.Row="0" ItemsSource="{Binding Scenes}" AutoGenerateColumns="False" HeadersVisibility="Column"
                              Style="{StaticResource CustomDataGridStyle}"
                              CellStyle="{StaticResource CustomDataGridCellStyle}"
                              ColumnHeaderStyle="{StaticResource CustomDataGridColumnHeaderStyle}"
                              RowStyle="{StaticResource CustomDataGridRowStyle}"
                              CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                              SelectionMode="Single" SelectionUnit="FullRow"
                              SelectedItem="{Binding MapSelectedScene, Mode=TwoWay}"
                              SelectionChanged="DataGrid_SelectionChanged"
                              CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" SortMemberPath="Id"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding Name}" SortMemberPath="Name"/>
                            <DataGridTextColumn Header="Type" Binding="{Binding SceneType}" SortMemberPath="SceneType"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" SortMemberPath="Description"/>
                            <DataGridTextColumn Header="Indoor" Binding="{Binding IsIndoor}" SortMemberPath="IsIndoor"/>
                            <DataGridTextColumn Header="Parent ID" Binding="{Binding ParentSceneId}" SortMemberPath="ParentSceneId"/>
                            <DataGridTextColumn Header="X" Binding="{Binding X}" SortMemberPath="X"/>
                            <DataGridTextColumn Header="Y" Binding="{Binding Y}" SortMemberPath="Y"/>
                            <DataGridTextColumn Header="Hub IDs" Binding="{Binding HubSceneIds, Converter={StaticResource IntListToStringConverter}}" SortMemberPath="HubSceneIds"/>
                            <DataGridTextColumn Header="Pop" Binding="{Binding Population}" SortMemberPath="Population"/>
                            <DataGridTextColumn Header="Radius" Binding="{Binding Radius}" SortMemberPath="Radius"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <Border Grid.Row="1" BorderBrush="{StaticResource PrimaryHueMidBrush}" BorderThickness="0,1,0,0" Margin="0,5,0,0">
                        <local:InteractiveMapView x:Name="InteractiveMap" DataContext="{Binding}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</UserControl>
