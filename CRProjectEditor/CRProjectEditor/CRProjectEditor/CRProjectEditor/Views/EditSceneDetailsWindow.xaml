<Window x:Class="CRProjectEditor.Views.EditSceneDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Редактировать Сцену" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}" 
        Foreground="{StaticResource ForegroundBrush}"
        FontFamily="{StaticResource DefaultFontFamily}"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Имя Сцены:" Margin="0,0,0,5"/>
        <TextBox x:Name="SceneNameTextBox" Grid.Row="0" Margin="0,20,0,10" />

        <TextBlock Text="Описание Сцены:" Grid.Row="1" Margin="0,0,0,5"/>
        <TextBox x:Name="SceneDescriptionTextBox" Grid.Row="1" Margin="0,20,0,10" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" MinHeight="80"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="SaveButton" Content="Сохранить" Width="100" Margin="0,0,10,0" Click="SaveButton_Click" />
            <Button x:Name="CancelButton" Content="Отмена" Width="100" IsCancel="True" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window> 