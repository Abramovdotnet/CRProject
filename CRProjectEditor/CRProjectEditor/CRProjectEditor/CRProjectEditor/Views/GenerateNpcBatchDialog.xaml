<Window x:Class="CRProjectEditor.Views.GenerateNpcBatchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Генерация NPC" Height="750" Width="1200"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource {x:Type Window}}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="600"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="GeneratedNpcsDataGrid"
                  ItemsSource="{Binding GeneratedNpcs}"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  Margin="10"
                  Style="{StaticResource {x:Type DataGrid}}"
                  Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTemplateColumn Width="40" IsReadOnly="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="✖" Command="{Binding DataContext.RemoveGeneratedNpcCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Style="{StaticResource {x:Type Button}}" Padding="0" Width="24" Height="24"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10" Grid.Row="1">
            <Button Content="Generate" Command="{Binding SaveGeneratedNpcsCommand}" Width="100" Margin="5" Style="{StaticResource {x:Type Button}}"/>
            <Button Content="Cancel" Command="{Binding CancelGeneratedNpcsCommand}" Width="100" Margin="5" Style="{StaticResource {x:Type Button}}"/>
        </StackPanel>
    </Grid>
</Window> 