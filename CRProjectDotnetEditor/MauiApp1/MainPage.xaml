<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiApp1"
             x:Class="MauiApp1.MainPage">

    <!-- BindingContext for MainViewModel can be removed if MainViewModel is truly empty 
         or if this page is no longer directly used or needs a global VM.
    <ContentPage.BindingContext>
        <local:MainViewModel />
    </ContentPage.BindingContext>
    -->

    <VerticalStackLayout>
        <Label 
            Text="This is MainPage. It's currently not directly used by the TabBar."
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
    </VerticalStackLayout>

</ContentPage>
